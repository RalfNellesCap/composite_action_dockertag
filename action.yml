name: 'composite_action_dockertag'
description: 'macro read branchname output Dockertag and Dockertag SHA'

runs:
  using: "composite"
  steps:
    - run: echo ${DOCKER_TAG_POSTFIX}
      shell: bash
